<div class="col-xs-12 col-sm-8 col-sm-offset-2">
	<div class="secondary-nav secondary-nav-invert">
		<i class="fa fa-2x fa-clipboard content-list-icon-primary pull-left"></i>
		<h2 class="font-invert"><b>End of Game Surveys (1/<%= @total_surveys %>)</b></h2>
		<h3 class="font-invert">Survey</h3>
	</div>

	<div class="content-container">
	  <div class="panel-secondary">
	    <div class="panel-title">
	      Team Member's Skills
	    </div>
	  </div>

	  <div class="panel-body">

	  	<div class="center" style="margin:10px 0;">
	  		<b>Tell us what was each of your fellow player's skill levels.</b> 
	  		<br>Your responses here will not affect your game performance in any way.
	  		<br>Please try to remember as much as possible. 
	  	</div>

			<%= form_tag "/players/#{@player.id}/update_measure_austin" do %>
		    <table class="table table-condensed table-hover" style="margin:30px 0px;">
		      <thead>
		        <tr>
		          <th></th>
		          <% 4.times do |i| %>
		            <th class="center progress-bar-<%= i+1 %>"><%= $SKILL_NAMES[i] %></th>
		          <% end %>
		        </tr>
		      </thead>

		      <tbody>
		        <% @player.measure_austin.skill_1_player_levels.each do |p_id| %>
		          <tr>
		            <td style="padding:15px;">
		              <b><%= @co_players.detect{ |x| x[0] == p_id[0] }[1] %></b>
		            </td>
		            <% 4.times do |i| %>
		              <td class="col-md-3" style="padding:12px 15px;">
		                <%= select_tag "input_skills[]", options_for_select(@measure_austin_options) %>
		              </td>
		            <% end %>
		          </tr>
		        <% end %>
		      </tbody>
		    </table>

			  <div class="center col-sm-6 col-sm-offset-3">
			    <%= button_tag(type: 'submit', class: "btn btn-block btn-green") do %>
			      SAVE & SUBMIT
			    <% end %>
			  </div>

			<% end %>

		</div>
	</div>

</div>
